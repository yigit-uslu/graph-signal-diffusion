# @package _global_
# PD sample collection config for the wra_medium scenario.
# Use with:
#   python -m graph_signal_diffusion.cli.wra.build_diffusion_dataset \
#     --config-name=pd_collection/wra_medium \
#     input_dir=/path/to/pd_run

defaults:
  - /pd_training/wra_medium
  - /pd_collection/defaults
  - _self_

# All collection parameters match pd_collection/defaults exactly.
# No overrides needed for wra_medium; provide ``input_dir=...`` on the CLI.
