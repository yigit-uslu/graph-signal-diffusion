# @package _global_
# PD sample collection config for the wra_medium_indoor_high_density scenario.
# Use with:
#   python -m graph_signal_diffusion.cli.wra.build_diffusion_dataset \
#     --config-name=pd_collection/wra_medium_indoor_high_density \
#     input_dir=/path/to/pd_run

defaults:
  - /pd_training/wra_medium_indoor_high_density
  - /pd_collection/defaults
  - _self_
